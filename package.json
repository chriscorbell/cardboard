{
  "name": "cardboard",
  "private": true,
  "packageManager": "pnpm@11.25.0",
  "engines": { "node": ">=24" },
  "scripts": {
    "dev": "pnpm --filter @cardboard/app dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "db:generate": "pnpm --filter @cardboard/app db:generate",
    "seed": "pnpm --filter @cardboard/app seed"
  }
}
