# Workspace memory

Read this index and [the protocol](protocol.md) when starting or resuming a session. Load topic notes only when their retrieval cue matches the task.

| When needed | Read |
| --- | --- |
| Workspace constraints, non-obvious structure, or recurring procedures | [Context](context/README.md) |
| A failure, gotcha, or previously corrected assumption | [Lessons](lessons/README.md) |
| Continuing unfinished work | [Work](work/README.md) |
| Writing or updating a memory note | [Note format](note-format.md) |
| Deciding which document owns a fact, whether a change owes a documentation edit, or repairing `AGENTS.md` | [Document maintenance](documents.md) |
| Finish step 4, a category over its threshold, or a requested memory review | [Bounded review](maintenance.md) |
| Several agents writing memory at once | [Concurrency](concurrency.md) |

## Canonical project documents

- [CONTEXT.md](../../CONTEXT.md): the glossary. Terms are used with these meanings everywhere.
- [Design](../design.md): the agreed v1 design, dated 2026-09-13, with links to the decision records.
- [Decision records](../adr/): eight ADRs covering identity, credentials, egress, storage, the runner, Approval, GitHub tokens, and the two-app merge authority.
- [Design review](../design-review.md): eight findings against the design, dated 2026-09-13, with a status header saying which are resolved. Read before implementing Previews or child-Card dispatch; it does not supersede accepted decisions.
- [GitHub Apps guide](../../deploy/github-apps.md) and the [cardboard-onboard skill](https://github.com/chriscorbell/skills/tree/main/cardboard-onboard): how a repository is prepared for a board.
- [README](../../README.md): how to run, build, and deploy; the Status section says what is not built.

## Review record

2026-09-15 (eighth): ordinary review at the end of the "Codex as a provider" card, which fixed a Codex Session path that had never run — the entrypoint used a flag codex-cli removed — and added a Codex route to the egress proxy. It follows the seventh record. The task added [`lessons/codex-cli-drifts-between-releases.md`](lessons/codex-cli-drifts-between-releases.md), the one durable finding here, and reconciled `work/2026-09-14-v1-gaps.md`: the Codex bullet moved from three unsolved problems to the Admin's remaining verification, and a new unverified-in-production entry says no Codex Session has ever run. Because it changed documented behaviour and an accepted decision's verification status, [the design](../design.md), [the README](../../README.md) and [ADR 0002](../adr/0002-subscription-credentials-stay-in-the-egress-proxy.md) were updated in the same change; ADR 0002's Codex exception now records exactly which half is verified and which needs the Admin's sign-in file. Sampling note: this Session read the cursor as `lessons/` entry 2 and sampled `lessons/` entries 2 to 4 before merging `main`, which brought the sixth and seventh records and moved the cursor to `work/` entry 1; the correct sample was then taken as well, so both are recorded. From the stale cursor: `lessons/docker-image-and-compose-gotchas.md` checked in part and retained, date unchanged — only the `init: true` bullet was re-read, confirmed on all four services in `deploy/compose.yaml` after this task edited that file. `lessons/session-token-cannot-push-workflow-files.md` checked and retained, date advanced to 2026-09-15 — the quoted `permissions` line in `mintInstallationToken` is unchanged, which is the note's own `Recheck when`. `lessons/check-the-pull-request-merges-before-reporting.md` checked and replayed, date advanced to 2026-09-15 — `main` moved four commits under this branch and conflicted in four files, and `gh pr view 8` answered `MERGEABLE`/`BLOCKED` after resolution, matching the note; its Source now cites this second occasion. From the current cursor, `work/` entry 1 is `work/2026-09-14-v1-gaps.md`, which the task already reconciled, so the sample wrapped to `context/`: `context/minicore-deployment-constraints.md` deferred again, date unchanged — verification needs SSH to minicore, which a Session cannot do, and it has now exceeded the incidental bound in four consecutive reviews, so closing it needs a deliberate pass from the Admin's machine rather than another sample. `context/cardboard-production-operations.md` checked and retained, date unchanged — `.github/workflows/ci.yml` still builds the five images it names, `agent` among them, and the entrypoint edit left Claude Code's `--output-format stream-json --verbose` untouched, so neither of the note's `Recheck when` conditions fired. `context/client-side-tests.md` checked and retained, date unchanged — `packages/app/package.json` still runs `client/test/*.test.ts` with no DOM harness; the test scripts this task added are in `packages/egress` and `packages/runner` and do not bear on it. No category is over its threshold: `context/` holds 4 entries and `lessons/` 5. Next cursor: `context/` entry 4.

2026-09-14 (seventh): ordinary review at the end of the "Live session transcripts" card, which made a Session's container log readable in the admin panel and switched the agent entrypoint to streaming output. It follows the sixth record, written concurrently on another branch from the same cursor, and adds to it rather than replacing it; both sampled `lessons/` entries 2 to 4, so the outcomes below are a second, independent reading of the same three notes. The task itself corrected `context/cardboard-production-operations.md`: its claim that "Claude Code in print mode writes its whole output at exit, so a running Session's log shows only the clone and start lines" was true and is now false by this change, so the bullet was rewritten with the date the behavior changed and a pointer to the runner's new `GET /sessions/:id/log`, and `Recheck when` gained the entrypoint's output format; verification date unchanged, same day. Everything else it changed went to the documents that own it — [the design](../design.md) Sessions section, which previously said no transcript appears in the UI, [the glossary](../../CONTEXT.md), which gained **Transcript**, and [the README](../../README.md) feature list — rather than into memory. `lessons/docker-image-and-compose-gotchas.md`: checked and retained, closing the part the sixth record left deferred — `packages/app/Dockerfile` still copies both `packages/shared` and `packages/shared/node_modules` into the runtime stage, which is the pair of image-build bullets; date unchanged, same day. `lessons/session-token-cannot-push-workflow-files.md`: retained as the sixth record corrected it — `mintInstallationToken` still narrows a `sessions` token to `contents`, `pull_requests`, `metadata`, so the Session-side conclusion holds, and this card kept its workflow-file edits at zero. `lessons/check-the-pull-request-merges-before-reporting.md`: checked and replayed rather than merely read — `main` moved under this branch mid-card and the prescribed `git fetch origin main && git merge origin/main` surfaced three conflicts, two of them in this index; date unchanged. `lessons/pnpm-11-build-approvals.md` held again here, `pnpm install --frozen-lockfile` running the esbuild postinstalls cleanly. Carried forward unchanged: `context/minicore-deployment-constraints.md` is still deferred for exceeding the incidental bound, and `work/2026-09-14-v1-gaps.md` stands as the sixth record left it — session transcripts were never one of its listed gaps. Next cursor: `work/` entry 1.

2026-09-14 (sixth): ordinary review at the end of the "Invitation emails" card, which made an Invitation send an email. It supersedes the fifth record of the same day and carries its open items forward. The task reconciled `work/2026-09-14-v1-gaps.md`: invitation emails moved off the open list and onto the unverified-in-production list, because no invitation has yet been delivered through Resend by the deployed app. It changed documented behavior, so [the design](../design.md) Notifications section and Status and [the README](../../README.md) feature list and Status were updated in the same change. Sampled from the fifth record's cursor, `lessons/` entry 2. `lessons/docker-image-and-compose-gotchas.md`: checked in part and retained — `deploy/compose.yaml` still carries `init: true` on four services and `packages/app/server/src/index.ts` still registers `/api/internal` before `/api`; the two image-build bullets exceeded the bound, so the verification date is unchanged. `lessons/session-token-cannot-push-workflow-files.md`: corrected against `services/github.ts` — the quoted `mintInstallationToken` body no longer matched the source, and the caveat it carried is answered there: only a `sessions` token narrows its permissions, so the merge token holds whatever the Merge App installation holds and a workflow-touching pull request is mergeable once that App has `workflows: write`. The Session-side conclusion is unchanged. `lessons/check-the-pull-request-merges-before-reporting.md`: checked and retained — followed in this Session, where `gh pr view 5 --json mergeable,mergeStateStatus` answered `MERGEABLE`/`BLOCKED`, the normal board state the note describes; date unchanged, same day. Correction to the two preceding records: `work/agents-md-proposal-acceptance-command-tests.md` does not exist, in `work/` or in its index, and `AGENTS.md` already runs `pnpm -r test` in the acceptance command, so the proposal it carried is satisfied and is no longer carried forward. Still carried forward unchanged: `context/minicore-deployment-constraints.md` is deferred for exceeding the incidental bound, and `context/cardboard-production-operations.md` stands as checked in the fifth record. Next cursor: `work/` entry 1.

