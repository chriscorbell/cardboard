# Remaining v1 gaps

Status: active
Objective: close what the design promised but the first implementation left out, in the order the user prefers. Branch `main`; Cardboard is itself a board, so cards on it may drive some of this work.

## Open, needing a user decision first

- Design-review finding 3, child-Card dispatch: Agent-created children never get a Session and the parent never resumes. Needs a rule for Agent-originated work without making every Agent action a Trigger.
~~Finding 5, Preview cookie scope, and finding 6, workload network isolation.~~ Answered 2026-09-15 by the "Runner-hosted previews" card: a single-use code issued by the app becomes a host-only Preview cookie at the Preview host, cookie and authorization headers are stripped before proxying, a per-Board `preview_epoch` revokes outstanding cookies when membership narrows, and Preview containers sit alone on a `cardboard_preview` network with no binds, no capabilities, and resource limits. The decision left for the user is only the hostname shape, below.

~~Provider fallback on usage-limit errors (design Q25).~~ Built 2026-09-15 by the "Provider fallback" card: the egress proxy records a 429 per provider with the reopening time its headers named and serves it at `/limits`; the app skips a provider whose window is still shut on dispatch, and on a failed Session whose provider refused during its life it writes a `provider_fallback` Trigger and re-dispatches on the other one. `sessions.fallback_from` makes it one hop. On by default, Admin switch in the agent tab.

## Open, no decision needed

- Retry control after a merge GitHub refused for a reason other than a changed head.
- ~~Runner-hosted Previews end to end: runner `/previews` returns 501, the app has no preview registry or cookie issuance.~~ Built 2026-09-15: the runner clones the branch, builds its Dockerfile and runs it; the app has a `previews` registry, the `request_preview` MCP tool, code issuance, and an hourly reaper; the preview router polls the routing table and enforces the cookie. The host setup was completed for Card mq729n on 2026-09-15 UTC: the free `{card}-preview.xode.cc` pattern, `*.xode.cc` DNS and tunnel rule, matching app/router secret, and preview network. The Board is now named kardboard and remains in runner mode. The later domain move changed the pattern to `{card}.kardboard.cc`; see [domain configuration](../../runbooks/domains.md). [PR 12](https://github.com/chriscorbell/kardboard/pull/12) merged and deployed as `f6b868b`, including PR 11's root Dockerfile. New branches inherit it; Card mq729n is Done.
- Documentation: smaller corrections from the design review. The safe SQLite snapshot procedure is answered by the "Backups" card — a daily verified `VACUUM INTO` snapshot and [the runbook](../runbooks/backups.md) — merged as `9d22c83` on 2026-09-14.
- ~~Nothing runs the test suite automatically.~~ Closed: `.github/workflows/ci.yml` now runs `pnpm -r test` in `validate`, between `typecheck` and `build`, and `AGENTS.md`'s acceptance command carries the same step. The edit was made outside a Session, as [the lesson](../lessons/session-token-cannot-push-workflow-files.md) predicted it must be; the card "Run the test suite in CI" is in Done.

Codex is done as of 2026-09-15: launch fixed (PR #8), sign-in file staged on the host with `chown 1000:1001` and mode 640, and inference routed through the egress proxy with no credential in the container, verified by two Sessions on the Sandbox board.

## Unverified in production

- Nightly sweep (first scheduled run 2026-09-15 03:00 America/New_York on the Sandbox and Cardboard boards).
- Member sign-up, the not-invited page, board-scoped access, and a Member's Approval.
- Cancel and re-run against a live container; boot reconciliation after a restart mid-Session.
- Whether Claude Code honours `CLAUDE_CODE_EFFORT_LEVEL` inside a Session.
- Any Codex Session at all: the path was broken until 2026-09-15 and has still not run on minicore. Whether the egress proxy's injected token is accepted by `chatgpt.com/backend-api/codex`, and whether its refresh against `auth.openai.com` works, both need the Admin's real sign-in file.
- Image attachments over MCP; Cloudflare Pages preview URL discovery.
- Backups on minicore: no snapshot has been taken by the deployed app, no snapshot has been restored, and the `backups/` directory's growth against the data volume is unobserved.
- Invitation emails: built by the "Invitation emails" card on 2026-09-14, but no invitation has been delivered by the deployed app through Resend.
- Runner-hosted Previews: the deployed membership and cookie flow passed public HTTPS checks with a temporary Member, including credential stripping and membership revocation. A real Member's Clerk sign-in remains unverified. Docker network separation was inspected; reachability through host-published ports and other LAN services remains unaudited. See [the runbook](../../runbooks/previews.md).
- Provider fallback: no real 429 has been observed through the deployed proxy, so neither the detection nor the re-dispatch has run on anything but tests. The `EGRESS_CONTROL_TOKEN` and `CARDBOARD_EGRESS_URL` wiring in `deploy/compose.yaml` cannot be proven before a merge either, since images build only on `main`.

Next action: approve PR 11 for the preview Dockerfile, then verify a real Member's Clerk sign-in and audit preview access to host-published ports. Child-Card dispatch still needs a decision.
Close when: every item above is built and observed, or explicitly moved out of scope in `docs/design.md`.
