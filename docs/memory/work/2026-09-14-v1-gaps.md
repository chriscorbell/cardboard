# Remaining v1 gaps

Status: active
Objective: close what the design promised but the first implementation left out, in the order the user prefers. Branch `main`; Cardboard is itself a board, so cards on it may drive some of this work.

## Open, needing a user decision first

- Design-review finding 3, child-Card dispatch: Agent-created children never get a Session and the parent never resumes. Needs a rule for Agent-originated work without making every Agent action a Trigger.
- Finding 5, Preview cookie scope, and finding 6, workload network isolation. Both bound to runner-hosted Previews.
- Provider fallback on usage-limit errors (design Q25): no detection, no fallback; a failed start fails the Session.

## Open, no decision needed

- Invitation emails: inviting a user only records the address.
- Retry control after a merge GitHub refused for a reason other than a changed head.
- Runner-hosted Previews end to end: runner `/previews` returns 501, the app has no preview registry or cookie issuance, the `*.preview.xode.cc` hostname and its certificate coverage are unresolved.
- Codex Provider: no sign-in file mounted, MCP config syntax unverified, no egress path.
- Documentation: smaller corrections from the design review. The safe SQLite snapshot procedure is answered by the "Backups" card — a daily verified `VACUUM INTO` snapshot and [the runbook](../runbooks/backups.md) — merged as `9d22c83` on 2026-09-14.
- ~~Nothing runs the test suite automatically.~~ Closed: `.github/workflows/ci.yml` now runs `pnpm -r test` in `validate`, between `typecheck` and `build`, and `AGENTS.md`'s acceptance command carries the same step. The edit was made outside a Session, as [the lesson](../lessons/session-token-cannot-push-workflow-files.md) predicted it must be; the card "Run the test suite in CI" is in Done.

## Unverified in production

- Nightly sweep (first scheduled run 2026-09-15 03:00 America/New_York on the Sandbox and Cardboard boards).
- Member sign-up, the not-invited page, board-scoped access, and a Member's Approval.
- Cancel and re-run against a live container; boot reconciliation after a restart mid-Session.
- Whether Claude Code honours `CLAUDE_CODE_EFFORT_LEVEL` inside a Session.
- Image attachments over MCP; Cloudflare Pages preview URL discovery.
- Backups on minicore: no snapshot has been taken by the deployed app, no snapshot has been restored, and the `backups/` directory's growth against the data volume is unobserved.

Next action: ask the user which of the decision items to settle first; provider fallback and invitation emails are the small wins.
Close when: every item above is built and observed, or explicitly moved out of scope in `docs/design.md`.
